// ---------------------------------------------------------
// c.mode — Cluster Mode Engine
// ---------------------------------------------------------
// Dieses Modul definiert alle Cluster-Modi und deckt seine
// Funktionen selbst auf. Keine Fragen, keine Abhängigkeiten.
// ---------------------------------------------------------

export const CMODE = {

    // -----------------------------------------------------
    // 1. Modus-Definitionen
    // -----------------------------------------------------
    MODES: {
        ICE:   { id: "ICE",   hue: 190, sat: 60, bright: 1.2, speed: 0.6 },
        WATER: { id: "WATER", hue: 210, sat: 70, bright: 1.1, speed: 0.8 },
        FIRE:  { id: "FIRE",  hue: 20,  sat: 90, bright: 1.4, speed: 1.3 }
    },

    // -----------------------------------------------------
    // 2. Aktueller Modus
    // -----------------------------------------------------
    current: "ICE",

    // -----------------------------------------------------
    // 3. Modus-Wechsel
    //
